// Página inicial que renderiza o componente HomeComponent

export const dynamic = "force-dynamic";

import HomeComponent from "@/components/Home";

export default function HomePage() {
  return <HomeComponent />;
}
